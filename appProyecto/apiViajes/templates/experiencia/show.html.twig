{% extends 'base.html.twig' %}

{% block title %}Experiencia{% endblock %}

{% block body %}
    <h1>Experiencia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ experiencium.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ experiencium.titulo }}</td>
            </tr>
            <tr>
                <th>Texto</th>
                <td>{{ experiencium.texto }}</td>
            </tr>
            <tr>
                <th>Puntuacion</th>
                <td>{{ experiencium.puntuacion }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ experiencium.fecha ? experiencium.fecha|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_experiencia_index') }}">back to list</a>

    <a href="{{ path('app_experiencia_edit', {'id': experiencium.id}) }}">edit</a>

    {{ include('experiencia/_delete_form.html.twig') }}
{% endblock %}
